# --- 웹앱 & 데이터 ---
streamlit>=1.36,<2
pandas>=2.1,<3
numpy==1.26.4          # 프로젝트 호환 선호 버전

# --- PDF 텍스트/표 추출 ---
PyMuPDF>=1.23,<1.25    # fitz
pdfminer.six>=20221105
pdfplumber>=0.11

# --- 표 추출(선택: 표가 많은 MSDS 권장) ---
camelot-py[cv]>=0.11   # OpenCV 의존 (lattice)
tabula-py>=2.9         # 자바 필요

# --- OCR 폴백(스캔본용, 선택) ---
pdf2image>=1.17
pillow>=10.3
pytesseract>=0.3.10

# --- 유틸 ---
rapidfuzz>=3.6
PyYAML>=6.0.1
